# 今日计划

## 1.改简历

## 2.修改自我介绍

## 3.总结别人的面经

## 4.卡尔曼滤波理解

```
/* 卡尔曼滤波器结构体 */
typedef struct
{
    float LastP;   // 上一时刻的状态方差（或协方差）
    float Now_P;   // 当前时刻的状态方差（或协方差）
    float out;     // 滤波器的输出值，即估计的状态
    float Kg;      // 卡尔曼增益，用于调节预测值和测量值之间的权重
    float Q;       // 过程噪声的方差，反映系统模型的不确定性
    float R;       // 测量噪声的方差，反映测量过程的不确定性
} KalmanFilter_Struct;
```

```
KalmanFilter_Struct kfs[3] = {
    {0.02, 0, 0, 0, 0.001, 0.543},
    {0.02, 0, 0, 0, 0.001, 0.543},
    {0.02, 0, 0, 0, 0.001, 0.543}};
double Common_Filter_KalmanFilter(KalmanFilter_Struct *kf, double input)
{
    kf->Now_P = kf->LastP + kf->Q;
    kf->Kg = kf->Now_P / (kf->Now_P + kf->R);
    kf->out = kf->out + kf->Kg * (input - kf->out);
    kf->LastP = (1 - kf->Kg) * kf->Now_P;
    return kf->out;
}
```

```
当前时刻的状态方差=上一次的状态方差+过程噪声方差
卡尔曼增益=当前状态的方差/(当前状态的方差+测量噪声方差)
输出值=输出值+卡尔曼增益*(输入值-上一次的输出值)
上一次的状态方差=(1-卡尔曼增益)*当前状态
```

```
大致理解了一点,我来复述一下,卡尔曼滤波有两个过程,分别是预测和更新,预测是计算当前次的不确定值等于上一次的不确定值加上预测这个过程的噪声不确定值,更新是,一是更新卡尔曼增益等于当前次的不确定值除以当前次的不确定值和测量噪声的不确定值,二是更新输出值等于上一次的输出值加上增益乘以测量值和上一次输出值的差值,三是更新上一次的不确定值等于当前次的不确定值乘以1-增益
```





